#!/bin/bash
#
# interactive-check-server-status — Outputs "Running", "Stopped", or ""
# Layer 2: delegates to layer1/admin/check-server-status
#

PROJECT_DIR="$(cd "$(dirname "$0")/../.." && pwd)"
source "$PROJECT_DIR/lib/common"
source "$PROJECT_DIR/lib/config"

bash "$PROJECT_DIR/layer1/admin/check-server-status"