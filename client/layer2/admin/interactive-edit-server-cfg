#!/bin/bash
#
# interactive-edit-server-cfg — Edit tes3mp-server-default.cfg on VPS
#

PROJECT_DIR="$(cd "$(dirname "$0")/../.." && pwd)"
source "$PROJECT_DIR/lib/common"
source "$PROJECT_DIR/lib/config"

bash "$PROJECT_DIR/layer1/admin/edit-server-cfg"