#!/bin/bash
#
# interactive-show-backups-mods — Show mod backup list from VPS
#

PROJECT_DIR="$(cd "$(dirname "$0")/../.." && pwd)"
source "$PROJECT_DIR/lib/common"
source "$PROJECT_DIR/lib/config"

bash "$PROJECT_DIR/layer1/admin/show-backups-mods"