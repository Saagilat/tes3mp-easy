#!/bin/bash
#
# interactive-check-restart-flag — Outputs "1" if server restart needed
# Layer 2: delegates to layer1/admin/check-restart-flag
#

PROJECT_DIR="$(cd "$(dirname "$0")/../.." && pwd)"
source "$PROJECT_DIR/lib/common"
source "$PROJECT_DIR/lib/config"

bash "$PROJECT_DIR/layer1/admin/check-restart-flag"